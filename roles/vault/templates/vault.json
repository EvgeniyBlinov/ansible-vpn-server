{
  "backend": {
    "file": {
      "path": "{{ vault__file_path }}"
    }
  },
  "listener": {
    "tcp": {
      "address": "0.0.0.0:{{vault__port }}",
      "tls_disable": 1
    }
  },
  "default_lease_ttl": "168h",
  "max_lease_ttl": "720h"
}
